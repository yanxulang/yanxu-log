引「../src/言录.yx」为 言录；

法 固定时间（）：数 则
    归 1700000000000；
终

定 内存 为 言录.内存处理器（言录.追踪级，言录.JSON行格式）；

定 日志 为 言录.日志器（言录.追踪级，「基准」，「批量」，【内存】，{「环境」：「性能」}）.设时间源（固定时间）；

令 序号：数 为 0；

当 （序号 小于 128） 则
    日志.信息于（「处理项目」，{「序号」：序号，「请求」：{「方法」：「GET」，「路径」：「/items」}，「标签」：【「言序」，「日志」】}）；
    置 序号 为 （序号 加 1）；
终

若 （内存.记录数（） 不等于 128） 则
    抛 「批量日志记录数不正确」；
终

言 「批量结构化日志：128 条」；
