引「../src/言录.yx」为 言录；

法 固定时间（）：数 则
    归 1700000000000；
终

定 内存 为 言录.内存处理器（言录.追踪级，言录.JSON行格式）；

定 日志 为 言录.日志器（言录.追踪级，「任务」，「」，【内存】，{}）.设时间源（固定时间）.固定采样（2，言录.警告级）；

令 序号：数 为 0；

当 （序号 小于 5） 则
    日志.信息于（「批处理进度」，{「序号」：序号}）；
    置 序号 为 （序号 加 1）；
终

日志.警告（「高等级记录不采样」）；

逐 正文：文 于 内存.各文本（） 则
    言 正文；
终

言 「采样日志示例完成」；
