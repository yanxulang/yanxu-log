引「../src/言录.yx」为 言录；

引「标准:文件」为 文件；

法 固定时间（）：数 则
    归 1700000000000；
终

定 路径：文 为 「.tmp-言录服务日志.jsonl」；

若 文件.存在（路径） 则
    文件.删除（路径，假）；
终

定 文件器 为 言录.JSON文件处理器（路径，言录.信息级，10485760，2）；

定 日志 为 言录.日志器（言录.追踪级，「订单服务」，「HTTP」，【言录.控制台处理器（言录.调试级），文件器】，{「环境」：「生产」}）.设时间源（固定时间）；

定 请求日志 为 日志.子日志器（「创建订单」，{「请求号」：「req-42」}）；

请求日志.信息于（「订单已创建」，{「订单号」：「o-100」，「令牌」：「不会泄漏」}）；

若 非 文件.存在（路径） 则
    抛 「示例日志文件未生成」；
终

文件.删除（路径，假）；
