引「包:言录」为 言录；

定 文件器 为 言录.JSON文件处理器（「应用.jsonl」，言录.信息级，10485760，5）；

定 日志 为 言录.日志器（言录.追踪级，「订单服务」，「HTTP」，【言录.控制台处理器（言录.调试级），文件器】，{「环境」：「生产」}）；

定 请求日志 为 日志.子日志器（「创建订单」，{「请求号」：「req-42」}）；

请求日志.信息于（「订单已创建」，{「订单号」：「o-100」，「令牌」：「不会泄漏」}）；
