引「../src/言录.yx」为 言录；

法 固定时间（）：数 则
    归 1700000000000；
终

定 内存 为 言录.内存处理器（言录.追踪级，言录.JSON行格式）；

定 日志 为 言录.日志器（言录.追踪级，「订单服务」，「」，【内存】，{「环境」：「演示」}）.设时间源（固定时间）；

日志.记请求（「请求完成」，「post」，「/orders?token=secret」，201，18，{「Authorization」：「Bearer secret」}，{「请求号」：「req-1」}）；

日志.记数据库（「查询完成」，「PostgreSQL」，「查询」，「SELECT id FROM orders WHERE id = $1」，7，1，{「id」：42，「password」：「secret」}，{「事务号」：「tx-1」}）；

逐 正文：文 于 内存.各文本（） 则
    言 正文；
终

言 「请求与数据库日志示例完成」；
