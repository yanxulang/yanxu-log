# 安全策略

## 支持范围

安全修复面向最新 1.x 稳定版本。报告问题时请提供言录版本、言序版本、最小复现、处理器类型和预期安全边界，不要附带真实凭据或生产日志。

## 权限

言录不申请网络、TCP、UDP、环境、进程或原生扩展权限。文件处理器需要文件能力；顶层应用应只授权专用日志目录，避免授予源码、密钥目录或用户主目录。

## 敏感数据

默认脱敏按典键名、忽略大小写、递归处理。它不是数据防泄漏扫描器：

- 消息文字不会内容扫描；
- 错误消息与踪迹不会内容扫描；
- SQL 语句模板不会内容扫描；
- 非默认业务键须由应用加入敏感键集合；
- 已经拼接进普通文字的凭据无法按键恢复。

应用不得把密码、令牌、Cookie、认证首部、连接串或私钥拼入自由文本。脱敏后的日志仍可能包含个人数据、业务标识和路径，须按数据治理要求保留与访问。

## 请求边界

`请求字段`移除查询和片段；绝对 HTTP 地址还移除权限部分。调用方仍应优先传路由模板或规范路径，不记录正文、表单、上传内容或完整 Cookie。

## 数据库边界

`数据库字段`递归脱敏参数，但不解析 SQL。只记录参数化语句模板；禁止记录拼接后的 SQL、数据库 URL 或驱动配置。需要诊断参数时，应使用最小、业务认可的允许列表。

## 资源拒绝服务

消息、字段、容器、节点、深度、处理器、记录字节、内存记录和轮转配置均有硬上限。深层输入使用非递归预检，以便在复制和脱敏前失败。超限错误不应通过无限重试放大日志风暴。

## 文件安全

- 父目录必须预先创建并设置最小权限；
- 一个路径只允许一个写入进程；
- 内置轮转不是原子事务，不适合作为不可丢失审计存储；
- 不要让不可信输入决定日志路径；
- 轮转备份继承目标目录的操作系统行为，部署时应单独验证权限和保留策略；
- 符号链接和挂载点由操作系统解析，应用应使用受控固定路径。

不可丢失、追加证明或法规审计场景应使用函数处理器接入专用审计系统，并独立监控投递结果。
