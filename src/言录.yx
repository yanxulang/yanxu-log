引「标准:JSON」为 JSON；

引「标准:时间」为 时间；

引「标准:文件」为 文件；

引「标准:文字」为 文字；

引「标准:数学」为 数学；

引「标准:字节」为 字节；

引「包:言据」为 言据；

公 定 版本：文 为 「0.1.0」；

公 定 追踪级：数 为 10；

公 定 调试级：数 为 20；

公 定 信息级：数 为 30；

公 定 警告级：数 为 40；

公 定 错误级：数 为 50；

公 定 致命级：数 为 60；

公 定 默认最大消息字符：数 为 16384；

公 定 默认最大名称字符：数 为 256；

公 定 默认最大容器项：数 为 4096；

公 定 默认最大字段项：数 为 256；

公 定 默认最大值节点：数 为 8192；

公 定 默认最大嵌套深度：数 为 8；

公 定 默认最大处理器数：数 为 32；

公 定 默认最大敏感键数：数 为 128；

公 定 默认最大内存记录数：数 为 4096；

公 定 默认最大记录字节：数 为 1048576；

公 定 默认最大路径字符：数 为 4096；

公 定 默认最大轮转字节：数 为 67108864；

公 定 默认最大轮转份数：数 为 128；

法 报错（代码：文，消息：文）：空 则
    抛 （（（「YANLU_」 加 代码） 加 「：」） 加 消息）；
终

法 找片（原文：文，片：文）：数? 则
    若 （长度（片） 等于 0） 则
        归 0；
    终
    令 位：数 为 0；
    当 （（位 加 长度（片）） 不大于 长度（原文）） 则
        若 （原文【位：（位 加 长度（片））】 等于 片） 则
            归 位；
        终
        置 位 为 （位 加 1）；
    终
    归 空；
终

公 法 错误详情（所误：误）：典 则
    定 消息：文 为 所误.消息；
    定 分隔：数? 为 找片（消息，「：」）；
    令 代码：文 为 「YANLU_RUNTIME」；
    令 说明：文 为 消息；
    若 （（分隔 是 数） 且 文字.始于（消息，「YANLU_」）） 则
        置 代码 为 消息【0：分隔】；
        置 说明 为 消息【（分隔 加 1）：】；
    终
    归 {「代码」：代码，「消息」：说明，「源代码」：所误.代码，「类别」：所误.类别，「位置」：所误.位置，「踪迹」：所误.踪迹}；
终

法 有所键（所典：典，键）：理 则
    归 包含（键列（所典），键）；
终

法 文字字节数（原文：文）：数 则
    归 字节.长度（字节.从文字（原文））；
终

法 检查日志值预算（来源）：空 则
    令 待办：列 为 【{「值」：来源，「深度」：0}】；
    令 位：数 为 0；
    令 总文字字节：数 为 0；
    当 （位 小于 长度（待办）） 则
        若 （长度（待办） 大于 默认最大值节点） 则
            报错（「LIMIT」，「日志值节点数超过安全上限」）；
        终
        定 节点：典 为 待办【位】；
        定 当前 为 节点【「值」】；
        定 深度：数 为 节点【「深度」】；
        若 （深度 大于 默认最大嵌套深度） 则
            报错（「LIMIT」，「日志值超过最大嵌套深度」）；
        终
        若 （当前 是 列） 则
            若 （长度（当前） 大于 默认最大容器项） 则
                报错（「LIMIT」，「日志列超过最大容器项数」）；
            终
            逐 项 于 当前 则
                追加（待办，{「值」：项，「深度」：（深度 加 1）}）；
            终
        否则
            若 （当前 是 典） 则
                若 （长度（当前） 大于 默认最大容器项） 则
                    报错（「LIMIT」，「日志典超过最大容器项数」）；
                终
                逐 键 于 当前 则
                    若 非 （键 是 文） 则
                        报错（「FIELD_KEY」，「日志典的键须为文字」）；
                    终
                    置 总文字字节 为 （总文字字节 加 文字字节数（键））；
                    追加（待办，{「值」：当前【键】，「深度」：（深度 加 1）}）；
                终
            否则
                若 非 包含（【「空」，「理」，「数」，「文」】，类型（当前）） 则
                    报错（「FIELD_VALUE」，（「日志值不可为」 加 类型（当前）））；
                终
                若 （当前 是 文） 则
                    置 总文字字节 为 （总文字字节 加 文字字节数（当前））；
                终
            终
        终
        若 （总文字字节 大于 默认最大记录字节） 则
            报错（「LIMIT」，「日志值文字总量超过单条记录预算」）；
        终
        置 位 为 （位 加 1）；
    终
    归 空；
终

法 复制值内（来源，深度：数） 则
    若 （来源 是 列） 则
        令 结果列：列 为 【】；
        逐 项 于 来源 则
            追加（结果列，复制值内（项，（深度 加 1）））；
        终
        归 结果列；
    终
    若 （来源 是 典） 则
        令 结果典：典 为 {}；
        逐 键 于 来源 则
            置 结果典【键】 为 复制值内（来源【键】，（深度 加 1））；
        终
        归 结果典；
    终
    归 来源；
终

法 复制值（来源） 则
    检查日志值预算（来源）；
    归 复制值内（来源，0）；
终

法 复制列（来源：列）：列 则
    检查日志值预算（来源）；
    令 结果：列 为 【】；
    逐 项 于 来源 则
        追加（结果，复制值内（项，1））；
    终
    归 结果；
终

法 复制典（来源：典）：典 则
    检查日志值预算（来源）；
    令 结果：典 为 {}；
    逐 键 于 来源 则
        置 结果【键】 为 复制值内（来源【键】，1）；
    终
    归 结果；
终

法 合并典（基础：典，覆盖：典）：典 则
    定 结果：典 为 复制典（基础）；
    检查日志值预算（覆盖）；
    逐 键 于 覆盖 则
        置 结果【键】 为 复制值内（覆盖【键】，1）；
    终
    若 （长度（结果） 大于 默认最大字段项） 则
        报错（「LIMIT」，「日志字段数量超过安全上限」）；
    终
    归 结果；
终

法 整数文（值：数）：文 则
    归 文字.替换（JSON.序列化（值），「.0」，「」）；
终

公 法 等级名（等级：数）：文 则
    若 （等级 不小于 致命级） 则
        归 「致命」；
    终
    若 （等级 不小于 错误级） 则
        归 「错误」；
    终
    若 （等级 不小于 警告级） 则
        归 「警告」；
    终
    若 （等级 不小于 信息级） 则
        归 「信息」；
    终
    若 （等级 不小于 调试级） 则
        归 「调试」；
    终
    归 「追踪」；
终

公 法 错误字段（所误：误）：典 则
    归 {「代码」：所误.代码，「类别」：所误.类别，「消息」：所误.消息，「位置」：所误.位置，「踪迹」：所误.踪迹}；
终

法 规范敏感键（敏感键：列）：列<文> 则
    若 （长度（敏感键） 大于 默认最大敏感键数） 则
        报错（「LIMIT_CONFIG」，「敏感键数量超过安全上限」）；
    终
    令 结果：列<文> 为 【】；
    逐 键 于 敏感键 则
        若 非 （键 是 文） 则
            报错（「REDACTION」，「敏感键须为文字」）；
        终
        若 （（长度（键） 等于 0） 或 （长度（键） 大于 默认最大名称字符）） 则
            报错（「REDACTION」，「敏感键须为 1 至 256 个字符」）；
        终
        定 小键：文 为 文字.小写（键）；
        若 非 包含（结果，小键） 则
            追加（结果，小键）；
        终
    终
    归 结果；
终

法 检查脱敏替代（替代：文）：空 则
    若 （长度（替代） 大于 默认最大名称字符） 则
        报错（「REDACTION」，「脱敏替代文字超过 256 个字符」）；
    终
    归 空；
终

法 脱敏值内（值，敏感键：列，替代：文） 则
    若 （值 是 典） 则
        令 结果：典 为 {}；
        逐 键 于 值 则
            若 包含（敏感键，文字.小写（键）） 则
                置 结果【键】 为 替代；
            否则
                置 结果【键】 为 脱敏值内（值【键】，敏感键，替代）；
            终
        终
        归 结果；
    终
    若 （值 是 列） 则
        令 结果：列 为 【】；
        逐 项 于 值 则
            追加（结果，脱敏值内（项，敏感键，替代））；
        终
        归 结果；
    终
    归 值；
终

公 法 脱敏（字段：典，敏感键：列<文>，替代：文）：典 则
    检查日志值预算（字段）；
    检查脱敏替代（替代）；
    归 脱敏值内（字段，规范敏感键（敏感键），替代）；
终

公 法 JSON行格式（记录：典）：文 则
    归 JSON.序列化（记录）；
终

公 法 言据格式（记录：典）：文 则
    归 言据.序列化（记录）；
终

公 法 控制台格式（记录：典）：文 则
    令 前缀：文 为 （（「[」 加 整数文（记录【「时间戳」】）） 加 「]」）；
    置 前缀 为 （（前缀 加 「 」） 加 记录【「等级」】）；
    若 （记录【「模块」】 不等于 「」） 则
        置 前缀 为 （（（前缀 加 「 [」） 加 记录【「模块」】） 加 「]」）；
    终
    若 （记录【「频道」】 不等于 「」） 则
        置 前缀 为 （（（前缀 加 「 <」） 加 记录【「频道」】） 加 「>」）；
    终
    令 结果：文 为 （（前缀 加 「 」） 加 记录【「消息」】）；
    若 （长度（记录【「字段」】） 大于 0） 则
        置 结果 为 （（结果 加 「 」） 加 JSON.序列化（记录【「字段」】））；
    终
    若 有所键（记录，「错误」） 则
        置 结果 为 （（结果 加 「 错误=」） 加 JSON.序列化（记录【「错误」】））；
    终
    归 结果；
终

公 协 日志处理器 则
    法 处理（记录：典）：空；
终

公 类 容错处理器 纳 日志处理器 则
    私 域 内层：日志处理器；
    私 域 失败数值：数；
    私 域 最近错误值：典；
    公 法 初始化（内层：日志处理器） 则
        置 此.内层 为 内层；
        置 此.失败数值 为 0；
        置 此.最近错误值 为 {}；
    终
    公 法 处理（记录：典）：空 则
        试 则
            此.内层.处理（复制典（记录））；
        救 所误 则
            置 此.失败数值 为 （此.失败数值 加 1）；
            置 此.最近错误值 为 错误详情（所误）；
        终
        归 空；
    终
    公 法 失败数（）：数 则
        归 此.失败数值；
    终
    公 法 最近错误（）：典? 则
        若 （长度（此.最近错误值） 等于 0） 则
            归 空；
        终
        归 复制典（此.最近错误值）；
    终
    公 法 清错误（）：容错处理器 则
        置 此.失败数值 为 0；
        置 此.最近错误值 为 {}；
        归 此；
    终
终

法 检查格式文本（正文：文）：空 则
    若 （文字字节数（正文） 大于 默认最大记录字节） 则
        报错（「LIMIT」，「格式化日志超过单条记录字节预算」）；
    终
    归 空；
终

公 类 函数处理器 纳 日志处理器 则
    公 只 域 最低等级：数；
    私 域 格式化法：法；
    私 域 写出法：法；
    公 法 初始化（最低等级：数，格式化法：法，写出法：法） 则
        置 此.最低等级 为 最低等级；
        置 此.格式化法 为 格式化法；
        置 此.写出法 为 写出法；
    终
    公 法 处理（记录：典）：空 则
        若 （记录【「等级值」】 不小于 此.最低等级） 则
            定 正文：文 为 此.格式化法（记录）；
            检查格式文本（正文）；
            此.写出法（正文，复制典（记录））；
        终
        归 空；
    终
终

法 写控制台（正文：文，记录：典）：空 则
    言 正文；
    归 空；
终

公 法 控制台处理器（最低等级：数）：函数处理器 则
    归 函数处理器（最低等级，控制台格式，写控制台）；
终

公 类 内存处理器 纳 日志处理器 则
    公 只 域 最低等级：数；
    私 域 格式化法：法；
    私 域 文本列：列<文>；
    私 域 记录列值：列<典>；
    公 法 初始化（最低等级：数，格式化法：法） 则
        置 此.最低等级 为 最低等级；
        置 此.格式化法 为 格式化法；
        置 此.文本列 为 【】；
        置 此.记录列值 为 【】；
    终
    公 法 处理（记录：典）：空 则
        若 （记录【「等级值」】 不小于 此.最低等级） 则
            若 （长度（此.记录列值） 不小于 默认最大内存记录数） 则
                报错（「LIMIT」，「内存处理器记录数超过安全上限」）；
            终
            定 正文：文 为 此.格式化法（记录）；
            检查格式文本（正文）；
            追加（此.文本列，正文）；
            追加（此.记录列值，复制典（记录））；
        终
        归 空；
    终
    公 法 各文本（）：列<文> 则
        归 复制列（此.文本列）；
    终
    公 法 各记录（）：列<典> 则
        归 复制列（此.记录列值）；
    终
    公 法 清空（）：空 则
        置 此.文本列 为 【】；
        置 此.记录列值 为 【】；
        归 空；
    终
终

公 类 文件处理器 纳 日志处理器 则
    公 只 域 路径：文；
    公 只 域 最低等级：数；
    公 只 域 轮转字节：数；
    公 只 域 保留份数：数；
    私 域 格式化法：法；
    公 法 初始化（路径：文，最低等级：数，格式化法：法，轮转字节：数，保留份数：数） 则
        若 （（长度（路径） 等于 0） 或 （长度（路径） 大于 默认最大路径字符）） 则
            报错（「PATH」，「日志路径须为 1 至 4096 个字符」）；
        终
        若 （（轮转字节 小于 0） 或 （数学.下取整（轮转字节） 不等于 轮转字节）） 则
            报错（「ROTATION」，「轮转字节须为非负整数」）；
        终
        若 （轮转字节 大于 默认最大轮转字节） 则
            报错（「LIMIT_CONFIG」，「轮转字节超过 64 MiB 安全上限」）；
        终
        若 （（保留份数 小于 0） 或 （数学.下取整（保留份数） 不等于 保留份数）） 则
            报错（「ROTATION」，「保留份数须为非负整数」）；
        终
        若 （保留份数 大于 默认最大轮转份数） 则
            报错（「LIMIT_CONFIG」，「轮转保留份数超过安全上限」）；
        终
        置 此.路径 为 路径；
        置 此.最低等级 为 最低等级；
        置 此.格式化法 为 格式化法；
        置 此.轮转字节 为 轮转字节；
        置 此.保留份数 为 保留份数；
    终
    私 法 备份路径（序号：数）：文 则
        归 （（此.路径 加 「.」） 加 整数文（序号））；
    终
    私 法 轮转（）：空 则
        若 （此.保留份数 大于 0） 则
            令 序号：数 为 此.保留份数；
            当 （序号 大于 1） 则
                定 前项：文 为 此.备份路径（（序号 减 1））；
                定 后项：文 为 此.备份路径（序号）；
                若 文件.存在（前项） 则
                    文件.写入（后项，文件.读取（前项））；
                否则
                    若 文件.存在（后项） 则
                        文件.删除（后项，假）；
                    终
                终
                置 序号 为 （序号 减 1）；
            终
            文件.写入（此.备份路径（1），文件.读取（此.路径））；
        终
        文件.写入（此.路径，「」）；
        归 空；
    终
    公 法 处理（记录：典）：空 则
        若 （记录【「等级值」】 小于 此.最低等级） 则
            归 空；
        终
        定 正文：文 为 （此.格式化法（记录） 加 「\n」）；
        检查格式文本（正文）；
        定 正文字节：数 为 文字字节数（正文）；
        若 （此.轮转字节 大于 0） 则
            若 文件.存在（此.路径） 则
                定 状态：典 为 文件.状态（此.路径）；
                若 （状态【「种类」】 不等于 「文件」） 则
                    报错（「PATH」，「日志路径已存在且不是普通文件」）；
                终
                若 （（状态【「字节数」】 加 正文字节） 大于 此.轮转字节） 则
                    此.轮转（）；
                终
            终
        终
        文件.追加（此.路径，正文）；
        归 空；
    终
终

公 法 JSON文件处理器（路径：文，最低等级：数，轮转字节：数，保留份数：数）：文件处理器 则
    归 文件处理器（路径，最低等级，JSON行格式，轮转字节，保留份数）；
终

公 法 言据文件处理器（路径：文，最低等级：数，轮转字节：数，保留份数：数）：文件处理器 则
    归 文件处理器（路径，最低等级，言据格式，轮转字节，保留份数）；
终

法 检查等级（等级：数）：空 则
    若 （数学.下取整（等级） 不等于 等级） 则
        报错（「LEVEL」，「日志等级须为整数」）；
    终
    若 （数学.绝对值（等级） 大于 1000000） 则
        报错（「LEVEL」，「日志等级超过安全范围」）；
    终
    归 空；
终

法 检查名称（名称：文，所类：文）：空 则
    若 （长度（名称） 大于 默认最大名称字符） 则
        报错（「LIMIT」，（所类 加 「超过 256 个字符」））；
    终
    归 空；
终

法 检查消息（消息：文）：空 则
    若 （长度（消息） 大于 默认最大消息字符） 则
        报错（「LIMIT」，「日志消息超过最大字符数」）；
    终
    归 空；
终

法 检查记录编码（记录：典）：空 则
    检查日志值预算（记录）；
    令 编码：文 为 「」；
    试 则
        置 编码 为 JSON.序列化（记录）；
    救 所误 则
        报错（「FIELD_VALUE」，（「日志记录不能编码为 JSON：」 加 所误.消息））；
    终
    若 （文字字节数（编码） 大于 默认最大记录字节） 则
        报错（「LIMIT」，「编码后的日志记录超过字节预算」）；
    终
    归 空；
终

法 系统时间毫秒（）：数 则
    归 时间.毫秒（）；
终

公 协 日志采样器 则
    法 保留（记录：典）：理；
终

公 类 全量采样器 纳 日志采样器 则
    公 法 初始化（） 则
    终
    公 法 保留（记录：典）：理 则
        归 真；
    终
终

公 类 固定间隔采样器 纳 日志采样器 则
    公 只 域 间隔：数；
    公 只 域 始终保留等级：数；
    私 域 位置：数；
    私 域 已检查值：数；
    私 域 已保留值：数；
    公 法 初始化（间隔：数，始终保留等级：数） 则
        若 （（间隔 小于 1） 或 （数学.下取整（间隔） 不等于 间隔）） 则
            报错（「SAMPLER_CONFIG」，「采样间隔须为正整数」）；
        终
        若 （间隔 大于 1000000） 则
            报错（「LIMIT_CONFIG」，「采样间隔超过安全上限」）；
        终
        检查等级（始终保留等级）；
        置 此.间隔 为 间隔；
        置 此.始终保留等级 为 始终保留等级；
        置 此.位置 为 0；
        置 此.已检查值 为 0；
        置 此.已保留值 为 0；
    终
    私 法 计检查（）：空 则
        若 （此.已检查值 小于 9007199254740991） 则
            置 此.已检查值 为 （此.已检查值 加 1）；
        终
        归 空；
    终
    私 法 计保留（）：空 则
        若 （此.已保留值 小于 9007199254740991） 则
            置 此.已保留值 为 （此.已保留值 加 1）；
        终
        归 空；
    终
    公 法 保留（记录：典）：理 则
        此.计检查（）；
        若 （记录【「等级值」】 不小于 此.始终保留等级） 则
            此.计保留（）；
            归 真；
        终
        定 应留：理 为 （此.位置 等于 0）；
        置 此.位置 为 （此.位置 加 1）；
        若 （此.位置 不小于 此.间隔） 则
            置 此.位置 为 0；
        终
        若 应留 则
            此.计保留（）；
        终
        归 应留；
    终
    公 法 已检查（）：数 则
        归 此.已检查值；
    终
    公 法 已保留（）：数 则
        归 此.已保留值；
    终
    公 法 重置（）：固定间隔采样器 则
        置 此.位置 为 0；
        置 此.已检查值 为 0；
        置 此.已保留值 为 0；
        归 此；
    终
终

公 类 日志器 则
    私 域 最低等级值：数；
    私 域 模块值：文；
    私 域 频道值：文；
    私 域 上下文字段：典；
    私 域 各处理器：列<日志处理器>；
    私 域 敏感键：列<文>；
    私 域 脱敏替代：文；
    私 域 时间法：法；
    私 域 采样器值：任意；
    公 法 初始化（最低等级：数，模块：文，频道：文，处理器列：列，上下文：典） 则
        检查等级（最低等级）；
        检查名称（模块，「模块」）；
        检查名称（频道，「频道」）；
        若 （长度（上下文） 大于 默认最大字段项） 则
            报错（「LIMIT」，「日志上下文字段超过安全上限」）；
        终
        若 （长度（处理器列） 大于 默认最大处理器数） 则
            报错（「LIMIT_CONFIG」，「日志处理器数量超过安全上限」）；
        终
        置 此.最低等级值 为 最低等级；
        置 此.模块值 为 模块；
        置 此.频道值 为 频道；
        置 此.上下文字段 为 复制典（上下文）；
        置 此.各处理器 为 【】；
        逐 处理器 于 处理器列 则
            若 非 （处理器 是 日志处理器） 则
                报错（「HANDLER」，「处理器未纳入日志处理器协议」）；
            终
            追加（此.各处理器，处理器）；
        终
        置 此.敏感键 为 规范敏感键（【「密码」，「口令」，「令牌」，「password」，「passwd」，「token」，「authorization」，「proxy-authorization」，「cookie」，「set-cookie」，「密钥」，「secret」，「credential」，「api-key」，「x-api-key」】）；
        置 此.脱敏替代 为 「***」；
        置 此.时间法 为 系统时间毫秒；
        置 此.采样器值 为 全量采样器（）；
    终
    公 法 设等级（最低等级：数）：日志器 则
        检查等级（最低等级）；
        置 此.最低等级值 为 最低等级；
        归 此；
    终
    公 法 加处理器（处理器：日志处理器）：日志器 则
        若 （长度（此.各处理器） 不小于 默认最大处理器数） 则
            报错（「LIMIT_CONFIG」，「日志处理器数量超过安全上限」）；
        终
        追加（此.各处理器，处理器）；
        归 此；
    终
    公 法 清处理器（）：日志器 则
        置 此.各处理器 为 【】；
        归 此；
    终
    公 法 设脱敏（敏感键：列<文>，替代：文）：日志器 则
        检查脱敏替代（替代）；
        置 此.敏感键 为 规范敏感键（敏感键）；
        置 此.脱敏替代 为 替代；
        归 此；
    终
    公 法 设时间源（时间源：法）：日志器 则
        置 此.时间法 为 时间源；
        归 此；
    终
    公 法 设采样器（采样器）：日志器 则
        置 此.采样器值 为 采样器；
        归 此；
    终
    公 法 固定采样（间隔：数，始终保留等级：数）：日志器 则
        置 此.采样器值 为 固定间隔采样器（间隔，始终保留等级）；
        归 此；
    终
    公 法 子日志器（模块：文，上下文：典）：日志器 则
        令 子模块：文 为 模块；
        若 （此.模块值 不等于 「」） 则
            若 （模块 等于 「」） 则
                置 子模块 为 此.模块值；
            否则
                置 子模块 为 （（此.模块值 加 「.」） 加 模块）；
            终
        终
        定 子：日志器 为 日志器（此.最低等级值，子模块，此.频道值，此.各处理器，合并典（此.上下文字段，上下文））；
        子.设脱敏（此.敏感键，此.脱敏替代）；
        子.设时间源（此.时间法）；
        子.设采样器（此.采样器值）；
        归 子；
    终
    公 法 频道（频道：文）：日志器 则
        定 子：日志器 为 日志器（此.最低等级值，此.模块值，频道，此.各处理器，此.上下文字段）；
        子.设脱敏（此.敏感键，此.脱敏替代）；
        子.设时间源（此.时间法）；
        子.设采样器（此.采样器值）；
        归 子；
    终
    公 法 上下文（字段：典）：日志器 则
        归 此.子日志器（「」，字段）；
    终
    公 法 写（等级：数，消息：文，字段：典，所误：误?）：典? 则
        检查等级（等级）；
        若 （等级 小于 此.最低等级值） 则
            归 空；
        终
        检查消息（消息）；
        若 （长度（字段） 大于 默认最大字段项） 则
            报错（「LIMIT」，「单次日志字段超过安全上限」）；
        终
        定 合并字段：典 为 合并典（此.上下文字段，字段）；
        定 时间戳：数 为 此.时间法（）；
        若 （（时间戳 小于 0） 或 （数学.下取整（时间戳） 不等于 时间戳）） 则
            报错（「TIME_SOURCE」，「时间源须返回非负整数毫秒」）；
        终
        定 记录：典 为 {「时间戳」：时间戳，「等级」：等级名（等级），「等级值」：等级，「消息」：消息，「模块」：此.模块值，「频道」：此.频道值，「字段」：脱敏（合并字段，此.敏感键，此.脱敏替代）}；
        若 （所误 是 误） 则
            置 记录【「错误」】 为 错误字段（所误）；
        终
        检查记录编码（记录）；
        令 应保留：理 为 假；
        试 则
            置 应保留 为 此.采样器值.保留（复制典（记录））；
        救 采样错误 则
            报错（「SAMPLER」，（「日志采样器执行失败：」 加 采样错误.消息））；
        终
        若 非 应保留 则
            归 空；
        终
        逐 处理器：日志处理器 于 此.各处理器 则
            处理器.处理（复制典（记录））；
        终
        归 复制典（记录）；
    终
    公 法 追踪（消息：文）：典? 则
        归 此.写（追踪级，消息，{}，空）；
    终
    公 法 追踪于（消息：文，字段：典）：典? 则
        归 此.写（追踪级，消息，字段，空）；
    终
    公 法 调试（消息：文）：典? 则
        归 此.写（调试级，消息，{}，空）；
    终
    公 法 调试于（消息：文，字段：典）：典? 则
        归 此.写（调试级，消息，字段，空）；
    终
    公 法 信息（消息：文）：典? 则
        归 此.写（信息级，消息，{}，空）；
    终
    公 法 信息于（消息：文，字段：典）：典? 则
        归 此.写（信息级，消息，字段，空）；
    终
    公 法 警告（消息：文）：典? 则
        归 此.写（警告级，消息，{}，空）；
    终
    公 法 警告于（消息：文，字段：典）：典? 则
        归 此.写（警告级，消息，字段，空）；
    终
    公 法 错误（消息：文）：典? 则
        归 此.写（错误级，消息，{}，空）；
    终
    公 法 错误于（消息：文，字段：典）：典? 则
        归 此.写（错误级，消息，字段，空）；
    终
    公 法 致命（消息：文）：典? 则
        归 此.写（致命级，消息，{}，空）；
    终
    公 法 致命于（消息：文，字段：典）：典? 则
        归 此.写（致命级，消息，字段，空）；
    终
    公 法 记错误（消息：文，所误：误，字段：典）：典? 则
        归 此.写（错误级，消息，字段，所误）；
    终
终

公 法 新建（最低等级：数）：日志器 则
    归 日志器（最低等级，「」，「」，【控制台处理器（最低等级）】，{}）；
终

公 法 静默（最低等级：数）：日志器 则
    归 日志器（最低等级，「」，「」，【】，{}）；
终
