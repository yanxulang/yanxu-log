引「标准:JSON」为 JSON；

引「标准:时间」为 时间；

引「标准:文件」为 文件；

引「标准:文字」为 文字；

引「包:言据」为 言据；

公 定 版本：文 为 「0.1.0」；

公 定 追踪级：数 为 10；

公 定 调试级：数 为 20；

公 定 信息级：数 为 30；

公 定 警告级：数 为 40；

公 定 错误级：数 为 50；

公 定 致命级：数 为 60；

法 有所键（所典：典，键）：理 则
    归 包含（键列（所典），键）；
终

法 复制列（来源：列）：列 则
    令 结果：列 为 【】；
    逐 项 于 来源 则
        追加（结果，项）；
    终
    归 结果；
终

法 复制典（来源：典）：典 则
    令 结果：典 为 {}；
    逐 键 于 来源 则
        置 结果【键】 为 来源【键】；
    终
    归 结果；
终

法 合并典（基础：典，覆盖：典）：典 则
    定 结果：典 为 复制典（基础）；
    逐 键 于 覆盖 则
        置 结果【键】 为 覆盖【键】；
    终
    归 结果；
终

法 整数文（值：数）：文 则
    归 文字.替换（JSON.序列化（值），「.0」，「」）；
终

公 法 等级名（等级：数）：文 则
    若 （等级 不小于 致命级） 则
        归 「致命」；
    终
    若 （等级 不小于 错误级） 则
        归 「错误」；
    终
    若 （等级 不小于 警告级） 则
        归 「警告」；
    终
    若 （等级 不小于 信息级） 则
        归 「信息」；
    终
    若 （等级 不小于 调试级） 则
        归 「调试」；
    终
    归 「追踪」；
终

公 法 错误字段（所误：误）：典 则
    归 {「代码」：所误.代码，「类别」：所误.类别，「消息」：所误.消息，「位置」：所误.位置，「踪迹」：所误.踪迹}；
终

法 脱敏值（值，敏感键：列，替代：文） 则
    若 （值 是 典） 则
        令 结果：典 为 {}；
        逐 键 于 值 则
            若 （（键 是 文） 且 包含（敏感键，键）） 则
                置 结果【键】 为 替代；
            否则
                置 结果【键】 为 脱敏值（值【键】，敏感键，替代）；
            终
        终
        归 结果；
    终
    若 （值 是 列） 则
        令 结果：列 为 【】；
        逐 项 于 值 则
            追加（结果，脱敏值（项，敏感键，替代））；
        终
        归 结果；
    终
    归 值；
终

公 法 脱敏（字段：典，敏感键：列<文>，替代：文）：典 则
    归 脱敏值（字段，敏感键，替代）；
终

公 法 JSON行格式（记录：典）：文 则
    归 JSON.序列化（记录）；
终

公 法 言据格式（记录：典）：文 则
    归 言据.序列化（记录）；
终

公 法 控制台格式（记录：典）：文 则
    令 前缀：文 为 （（「[」 加 整数文（记录【「时间戳」】）） 加 「]」）；
    置 前缀 为 （（前缀 加 「 」） 加 记录【「等级」】）；
    若 （记录【「模块」】 不等于 「」） 则
        置 前缀 为 （（（前缀 加 「 [」） 加 记录【「模块」】） 加 「]」）；
    终
    若 （记录【「频道」】 不等于 「」） 则
        置 前缀 为 （（（前缀 加 「 <」） 加 记录【「频道」】） 加 「>」）；
    终
    令 结果：文 为 （（前缀 加 「 」） 加 记录【「消息」】）；
    若 （长度（记录【「字段」】） 大于 0） 则
        置 结果 为 （（结果 加 「 」） 加 JSON.序列化（记录【「字段」】））；
    终
    若 有所键（记录，「错误」） 则
        置 结果 为 （（结果 加 「 错误=」） 加 JSON.序列化（记录【「错误」】））；
    终
    归 结果；
终

公 协 日志处理器 则
    法 处理（记录：典）：空；
终

公 类 函数处理器 纳 日志处理器 则
    公 只 域 最低等级：数；
    私 域 格式化法：法；
    私 域 写出法：法；
    公 法 初始化（最低等级：数，格式化法：法，写出法：法） 则
        置 此.最低等级 为 最低等级；
        置 此.格式化法 为 格式化法；
        置 此.写出法 为 写出法；
    终
    公 法 处理（记录：典）：空 则
        若 （记录【「等级值」】 不小于 此.最低等级） 则
            此.写出法（此.格式化法（记录），记录）；
        终
        归 空；
    终
终

法 写控制台（正文：文，记录：典）：空 则
    言 正文；
    归 空；
终

公 法 控制台处理器（最低等级：数）：函数处理器 则
    归 函数处理器（最低等级，控制台格式，写控制台）；
终

公 类 内存处理器 纳 日志处理器 则
    公 只 域 最低等级：数；
    私 域 格式化法：法；
    私 域 文本列：列<文>；
    私 域 记录列值：列<典>；
    公 法 初始化（最低等级：数，格式化法：法） 则
        置 此.最低等级 为 最低等级；
        置 此.格式化法 为 格式化法；
        置 此.文本列 为 【】；
        置 此.记录列值 为 【】；
    终
    公 法 处理（记录：典）：空 则
        若 （记录【「等级值」】 不小于 此.最低等级） 则
            追加（此.文本列，此.格式化法（记录））；
            追加（此.记录列值，记录）；
        终
        归 空；
    终
    公 法 各文本（）：列<文> 则
        归 复制列（此.文本列）；
    终
    公 法 各记录（）：列<典> 则
        归 复制列（此.记录列值）；
    终
    公 法 清空（）：空 则
        置 此.文本列 为 【】；
        置 此.记录列值 为 【】；
        归 空；
    终
终

公 类 文件处理器 纳 日志处理器 则
    公 只 域 路径：文；
    公 只 域 最低等级：数；
    公 只 域 轮转字节：数；
    公 只 域 保留份数：数；
    私 域 格式化法：法；
    公 法 初始化（路径：文，最低等级：数，格式化法：法，轮转字节：数，保留份数：数） 则
        若 （轮转字节 小于 0） 则
            抛 「YANLU_ROTATION：轮转字节不可为负数」；
        终
        若 （保留份数 小于 0） 则
            抛 「YANLU_ROTATION：保留份数不可为负数」；
        终
        置 此.路径 为 路径；
        置 此.最低等级 为 最低等级；
        置 此.格式化法 为 格式化法；
        置 此.轮转字节 为 轮转字节；
        置 此.保留份数 为 保留份数；
    终
    私 法 备份路径（序号：数）：文 则
        归 （（此.路径 加 「.」） 加 整数文（序号））；
    终
    私 法 轮转（）：空 则
        若 （此.保留份数 大于 0） 则
            令 序号：数 为 此.保留份数；
            当 （序号 大于 1） 则
                定 前项：文 为 此.备份路径（（序号 减 1））；
                定 后项：文 为 此.备份路径（序号）；
                若 文件.存在（前项） 则
                    文件.写入（后项，文件.读取（前项））；
                否则
                    若 文件.存在（后项） 则
                        文件.删除（后项，假）；
                    终
                终
                置 序号 为 （序号 减 1）；
            终
            文件.写入（此.备份路径（1），文件.读取（此.路径））；
        终
        文件.写入（此.路径，「」）；
        归 空；
    终
    公 法 处理（记录：典）：空 则
        若 （记录【「等级值」】 小于 此.最低等级） 则
            归 空；
        终
        定 正文：文 为 （此.格式化法（记录） 加 「\n」）；
        若 （此.轮转字节 大于 0） 则
            若 文件.存在（此.路径） 则
                定 状态：典 为 文件.状态（此.路径）；
                若 （（状态【「字节数」】 加 长度（正文）） 大于 此.轮转字节） 则
                    此.轮转（）；
                终
            终
        终
        文件.追加（此.路径，正文）；
        归 空；
    终
终

公 法 JSON文件处理器（路径：文，最低等级：数，轮转字节：数，保留份数：数）：文件处理器 则
    归 文件处理器（路径，最低等级，JSON行格式，轮转字节，保留份数）；
终

公 法 言据文件处理器（路径：文，最低等级：数，轮转字节：数，保留份数：数）：文件处理器 则
    归 文件处理器（路径，最低等级，言据格式，轮转字节，保留份数）；
终

公 类 日志器 则
    私 域 最低等级值：数；
    私 域 模块值：文；
    私 域 频道值：文；
    私 域 上下文字段：典；
    私 域 各处理器：列<日志处理器>；
    私 域 敏感键：列<文>；
    私 域 脱敏替代：文；
    公 法 初始化（最低等级：数，模块：文，频道：文，处理器列：列，上下文：典） 则
        置 此.最低等级值 为 最低等级；
        置 此.模块值 为 模块；
        置 此.频道值 为 频道；
        置 此.上下文字段 为 复制典（上下文）；
        置 此.各处理器 为 【】；
        逐 处理器 于 处理器列 则
            若 非 （处理器 是 日志处理器） 则
                抛 「YANLU_HANDLER：处理器未纳入日志处理器协议」；
            终
            追加（此.各处理器，处理器）；
        终
        置 此.敏感键 为 【「密码」，「口令」，「令牌」，「token」，「authorization」，「密钥」】；
        置 此.脱敏替代 为 「***」；
    终
    公 法 设等级（最低等级：数）：日志器 则
        置 此.最低等级值 为 最低等级；
        归 此；
    终
    公 法 加处理器（处理器：日志处理器）：日志器 则
        追加（此.各处理器，处理器）；
        归 此；
    终
    公 法 清处理器（）：日志器 则
        置 此.各处理器 为 【】；
        归 此；
    终
    公 法 设脱敏（敏感键：列<文>，替代：文）：日志器 则
        置 此.敏感键 为 复制列（敏感键）；
        置 此.脱敏替代 为 替代；
        归 此；
    终
    公 法 子日志器（模块：文，上下文：典）：日志器 则
        令 子模块：文 为 模块；
        若 （此.模块值 不等于 「」） 则
            若 （模块 等于 「」） 则
                置 子模块 为 此.模块值；
            否则
                置 子模块 为 （（此.模块值 加 「.」） 加 模块）；
            终
        终
        定 子：日志器 为 日志器（此.最低等级值，子模块，此.频道值，此.各处理器，合并典（此.上下文字段，上下文））；
        子.设脱敏（此.敏感键，此.脱敏替代）；
        归 子；
    终
    公 法 频道（频道：文）：日志器 则
        定 子：日志器 为 日志器（此.最低等级值，此.模块值，频道，此.各处理器，此.上下文字段）；
        子.设脱敏（此.敏感键，此.脱敏替代）；
        归 子；
    终
    公 法 上下文（字段：典）：日志器 则
        归 此.子日志器（「」，字段）；
    终
    公 法 写（等级：数，消息：文，字段：典，所误：误?）：典? 则
        若 （等级 小于 此.最低等级值） 则
            归 空；
        终
        定 合并字段：典 为 合并典（此.上下文字段，字段）；
        定 记录：典 为 {「时间戳」：时间.毫秒（），「等级」：等级名（等级），「等级值」：等级，「消息」：消息，「模块」：此.模块值，「频道」：此.频道值，「字段」：脱敏（合并字段，此.敏感键，此.脱敏替代）}；
        若 （所误 是 误） 则
            置 记录【「错误」】 为 错误字段（所误）；
        终
        逐 处理器：日志处理器 于 此.各处理器 则
            处理器.处理（记录）；
        终
        归 记录；
    终
    公 法 追踪（消息：文）：典? 则
        归 此.写（追踪级，消息，{}，空）；
    终
    公 法 追踪于（消息：文，字段：典）：典? 则
        归 此.写（追踪级，消息，字段，空）；
    终
    公 法 调试（消息：文）：典? 则
        归 此.写（调试级，消息，{}，空）；
    终
    公 法 调试于（消息：文，字段：典）：典? 则
        归 此.写（调试级，消息，字段，空）；
    终
    公 法 信息（消息：文）：典? 则
        归 此.写（信息级，消息，{}，空）；
    终
    公 法 信息于（消息：文，字段：典）：典? 则
        归 此.写（信息级，消息，字段，空）；
    终
    公 法 警告（消息：文）：典? 则
        归 此.写（警告级，消息，{}，空）；
    终
    公 法 警告于（消息：文，字段：典）：典? 则
        归 此.写（警告级，消息，字段，空）；
    终
    公 法 错误（消息：文）：典? 则
        归 此.写（错误级，消息，{}，空）；
    终
    公 法 错误于（消息：文，字段：典）：典? 则
        归 此.写（错误级，消息，字段，空）；
    终
    公 法 致命（消息：文）：典? 则
        归 此.写（致命级，消息，{}，空）；
    终
    公 法 致命于（消息：文，字段：典）：典? 则
        归 此.写（致命级，消息，字段，空）；
    终
    公 法 记错误（消息：文，所误：误，字段：典）：典? 则
        归 此.写（错误级，消息，字段，所误）；
    终
终

公 法 新建（最低等级：数）：日志器 则
    归 日志器（最低等级，「」，「」，【控制台处理器（最低等级）】，{}）；
终

公 法 静默（最低等级：数）：日志器 则
    归 日志器（最低等级，「」，「」，【】，{}）；
终
