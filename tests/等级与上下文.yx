引「../src/言录.yx」为 言录；

引「标准:测试」为 测试；

定 内存 为 言录.内存处理器（言录.追踪级，言录.JSON行格式）；

定 根 为 言录.日志器（言录.信息级，「服务」，「请求」，【内存】，{「应用」：「演示」，「令牌」：「秘密」}）；

定 子 为 根.子日志器（「用户」，{「请求号」：「r-1」}）；

测试.相等（子.调试（「不会记录」），空）；

子.信息于（「登录完成」，{「用户」：「子衿」，「嵌套」：{「密码」：「123456」}}）；

子.警告（「示例警告」）；

定 各记录 为 内存.各记录（）；

测试.相等（长度（各记录），2）；

测试.相等（各记录【0】【「模块」】，「服务.用户」）；

测试.相等（各记录【0】【「频道」】，「请求」）；

测试.相等（各记录【0】【「字段」】【「令牌」】，「***」）；

测试.相等（各记录【0】【「字段」】【「嵌套」】【「密码」】，「***」）；

测试.相等（各记录【1】【「等级」】，「警告」）；
