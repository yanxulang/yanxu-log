引「../src/言录.yx」为 言录；

引「标准:测试」为 测试；

法 须报（操作：法，代码：文）：空 则
    令 已报：理 为 假；
    令 详情：典 为 {}；
    试 则
        操作（）；
    救 所误 则
        置 已报 为 真；
        置 详情 为 言录.错误详情（所误）；
    终
    测试.断言（已报，（代码 加 「错误应被抛出」））；
    测试.相等（详情【「代码」】，代码）；
    归 空；
终

法 固定时间（）：数 则
    归 1700000000123；
终

法 坏时间（）：数 则
    归 减 1；
终

法 篡改输出（正文：文，记录：典）：空 则
    置 记录【「字段」】【「结果」】 为 「被处理器篡改」；
    归 空；
终

定 内存 为 言录.内存处理器（言录.追踪级，言录.JSON行格式）；

定 篡改器 为 言录.函数处理器（言录.追踪级，言录.JSON行格式，篡改输出）；

令 初始上下文：典 为 {「服务」：{「名称」：「订单」}，「Authorization」：「Bearer secret」}；

定 日志 为 言录.日志器（言录.追踪级，「边界」，「测试」，【篡改器，内存】，初始上下文）.设时间源（固定时间）；

置 初始上下文【「服务」】【「名称」】 为 「外部改写」；

令 字段：典 为 {「结果」：「成功」，「首部」：{「X-API-Key」：「key-1」}，「列」：【{「PASSWORD」：「p-1」}】}；

定 记录：典? 为 日志.信息于（「完成」，字段）；

置 字段【「结果」】 为 「外部改写」；

测试.相等（记录【「时间戳」】，1700000000123）；

测试.相等（记录【「字段」】【「服务」】【「名称」】，「订单」）；

测试.相等（记录【「字段」】【「Authorization」】，「***」）；

测试.相等（记录【「字段」】【「首部」】【「X-API-Key」】，「***」）；

测试.相等（记录【「字段」】【「列」】【0】【「PASSWORD」】，「***」）；

测试.相等（记录【「字段」】【「结果」】，「成功」）；

定 已存：列<典> 为 内存.各记录（）；

测试.相等（已存【0】【「字段」】【「结果」】，「成功」）；

置 已存【0】【「字段」】【「结果」】 为 「快照改写」；

测试.相等（内存.各记录（）【0】【「字段」】【「结果」】，「成功」）；

令 深值：典 为 {「值」：1}；

令 深度：数 为 0；

当 （深度 小于 12） 则
    置 深值 为 {「下一层」：深值}；
    置 深度 为 （深度 加 1）；
终

法 写过深（）：空 则
    日志.信息于（「过深」，{「数据」：深值}）；
    归 空；
终

须报（写过深，「YANLU_LIMIT」）；

令 非文键：典 为 {}；

置 非文键【1】 为 「值」；

法 写非文键（）：空 则
    日志.信息于（「坏键」，非文键）；
    归 空；
终

须报（写非文键，「YANLU_FIELD_KEY」）；

令 大消息：文 为 「甲」；

当 （长度（大消息） 不大于 言录.默认最大消息字符） 则
    置 大消息 为 （大消息 加 大消息）；
终

法 写大消息（）：空 则
    日志.信息（大消息）；
    归 空；
终

须报（写大消息，「YANLU_LIMIT」）；

定 坏时日志 为 言录.静默（言录.追踪级）.设时间源（坏时间）；

法 写坏时间（）：空 则
    坏时日志.信息（「时间」）；
    归 空；
终

须报（写坏时间，「YANLU_TIME_SOURCE」）；

法 坏轮转（）：空 则
    言录.JSON文件处理器（「tests/.tmp-坏轮转.jsonl」，言录.信息级，10.5，1）；
    归 空；
终

须报（坏轮转，「YANLU_ROTATION」）；

令 过多处理器：列 为 【】；

令 处理器数：数 为 0；

当 （处理器数 小于 33） 则
    追加（过多处理器，内存）；
    置 处理器数 为 （处理器数 加 1）；
终

法 建过多处理器（）：空 则
    言录.日志器（言录.信息级，「」，「」，过多处理器，{}）；
    归 空；
终

须报（建过多处理器，「YANLU_LIMIT_CONFIG」）；
