引「../src/言录.yx」为 言录；

引「标准:测试」为 测试；

引「标准:文件」为 文件；

引「标准:文字」为 文字；

定 JSON内存 为 言录.内存处理器（言录.追踪级，言录.JSON行格式）；

定 据内存 为 言录.内存处理器（言录.追踪级，言录.言据格式）；

定 记录器 为 言录.日志器（言录.追踪级，「测试」，「」，【JSON内存，据内存】，{}）；

记录器.追踪于（「开始」，{「序号」：1}）；

测试.断言（文字.始于（JSON内存.各文本（）【0】，「{」），「JSON Lines 应输出 JSON 对象」）；

测试.断言（文字.始于（据内存.各文本（）【0】，「据【」），「言据输出应为据值」）；

定 路径：文 为 「tests/.tmp-言录.jsonl」；

若 文件.存在（路径） 则
    文件.删除（路径，假）；
终

若 文件.存在（（路径 加 「.1」）） 则
    文件.删除（（路径 加 「.1」），假）；
终

定 文件器 为 言录.JSON文件处理器（路径，言录.信息级，80，1）；

定 文件日志 为 言录.日志器（言录.信息级，「轮转」，「」，【文件器】，{}）；

文件日志.信息于（「第一条足够长的结构化日志」，{「内容」：「天地玄黄宇宙洪荒」}）；

文件日志.信息于（「第二条触发轮转的结构化日志」，{「内容」：「日月盈昃辰宿列张」}）；

测试.断言（文件.存在（路径），「当前日志文件应存在」）；

测试.断言（文件.存在（（路径 加 「.1」）），「轮转备份应存在」）；

文件.删除（路径，假）；

文件.删除（（路径 加 「.1」），假）；
