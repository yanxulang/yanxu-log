引「../src/言录.yx」为 言录；

引「标准:JSON」为 JSON；

引「标准:测试」为 测试；

引「包:言据」为 言据；

法 须报（操作：法，代码：文）：空 则
    令 已报：理 为 假；
    令 详情：典 为 {}；
    试 则
        操作（）；
    救 所误 则
        置 已报 为 真；
        置 详情 为 言录.错误详情（所误）；
    终
    测试.断言（已报，（代码 加 「错误应被抛出」））；
    测试.相等（详情【「代码」】，代码）；
    归 空；
终

法 固定时间（）：数 则
    归 1700000000000；
终

定 请求：典 为 言录.请求字段（「get」，「https://user:pass@example.test/orders/42?token=secret#片段」，201，17，{「Authorization」：「Bearer secret」，「Cookie」：「sid=secret」，「Accept」：「application/vnd.yanxu.yanju」}，{「事件类型」：「不可覆盖」，「追踪号」：「trace-1」，「access_token」：「secret」}）；

测试.相等（请求【「事件类型」】，「HTTP请求」）；

测试.相等（请求【「请求」】【「方法」】，「GET」）；

测试.相等（请求【「请求」】【「目标」】，「/orders/42」）；

测试.相等（请求【「请求」】【「状态码」】，201）；

测试.相等（请求【「请求」】【「首部」】【「Authorization」】，「***」）；

测试.相等（请求【「请求」】【「首部」】【「Cookie」】，「***」）；

测试.相等（请求【「请求」】【「首部」】【「Accept」】，「application/vnd.yanxu.yanju」）；

测试.相等（请求【「追踪号」】，「trace-1」）；

测试.相等（请求【「access_token」】，「***」）；

定 数据库：典 为 言录.数据库字段（「PostgreSQL」，「查询」，「SELECT id FROM users WHERE id = $1」，9，1，{「id」：42，「PASSWORD」：「secret」}，{「事件类型」：「不可覆盖」，「事务号」：「tx-1」，「database_url」：「postgres://secret」}）；

测试.相等（数据库【「事件类型」】，「数据库」）；

测试.相等（数据库【「数据库」】【「系统」】，「PostgreSQL」）；

测试.相等（数据库【「数据库」】【「操作」】，「查询」）；

测试.相等（数据库【「数据库」】【「参数」】【「PASSWORD」】，「***」）；

测试.相等（数据库【「database_url」】，「***」）；

定 内存 为 言录.内存处理器（言录.追踪级，言录.JSON行格式）；

定 日志 为 言录.日志器（言录.追踪级，「集成」，「」，【内存】，{}）.设时间源（固定时间）；

日志.记请求（「请求完成」，「post」，「/items?password=secret」，204，3，{「X-API-Key」：「secret」}，{「请求号」：「r-1」}）；

日志.记数据库（「SQL 完成」，「SQLite」，「更新」，「UPDATE items SET name = ? WHERE id = ?」，4，1，{「name」：「言序」，「id」：1}，{「事务号」：「t-1」}）；

定 各记录：列<典> 为 内存.各记录（）；

测试.相等（长度（各记录），2）；

测试.相等（各记录【0】【「模式版本」】，1）；

测试.相等（各记录【0】【「字段」】【「请求」】【「目标」】，「/items」）；

测试.相等（各记录【0】【「字段」】【「请求」】【「首部」】【「X-API-Key」】，「***」）；

测试.相等（各记录【1】【「字段」】【「数据库」】【「影响行数」】，1）；

定 JSON往返：典 为 JSON.解析（言录.JSON行格式（各记录【0】））；

测试.相等（JSON往返【「字段」】【「请求」】【「目标」】，「/items」）；

定 言据往返：典 为 言据.解析据（言录.言据格式（各记录【1】））；

测试.相等（言据往返【「字段」】【「数据库」】【「系统」】，「SQLite」）；

法 坏状态（）：空 则
    言录.请求字段（「GET」，「/」，99，0，{}，{}）；
    归 空；
终

须报（坏状态，「YANLU_INTEGRATION」）；

法 坏耗时（）：空 则
    言录.请求字段（「GET」，「/」，200，减 1，{}，{}）；
    归 空；
终

须报（坏耗时，「YANLU_INTEGRATION」）；

法 坏数据库（）：空 则
    言录.数据库字段（「」，「查询」，「SELECT 1」，0，空，{}，{}）；
    归 空；
终

须报（坏数据库，「YANLU_INTEGRATION」）；
