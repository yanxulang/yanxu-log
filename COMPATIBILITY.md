# 兼容性

## 言序

言录 1.0 的最低言序版本是 1.1.6。源码、规格、示例、基准、锁图和 Release 构建同时在 1.1.6 与当前稳定线 1.1.8 验证。

| 能力 | 1.1.6 | 1.1.8 |
| --- | --- | --- |
| 树解释器 | 支持 | 支持 |
| 字节码 VM | 支持 | 支持 |
| 生成 API | 读取兼容 | 生成门禁 |
| Release YXB | 支持 | 支持 |

## 操作系统

纯内存、格式化、脱敏、采样和集成字段 API 在 Linux、macOS、Windows 上行为一致。CI 在三平台用言序 1.1.6 完成包清单、静态、规格、树/字节码、示例、基准、锁图和构建验证。

文件 API 使用 UTF-8 和平台原生路径，受顶层应用权限控制。路径分隔符、只读权限和文件被其他进程占用时的错误由操作系统决定。内置轮转不提供多进程锁。

## 言据

言录锁定言据`v1.2.0`，范围为`^1.2`。`言据格式`输出言据 v1`据`值；1.x 不改变该格式版本。

## 稳定范围

1.x 保证：

- 六个预定义等级值；
- 记录模式 v1 的基础字段和类型；
- 公开构造器、处理器、采样器和字段构造器签名；
- `YANLU_`错误代码前缀；
- 请求与数据库标准字段不会被扩展覆盖。

控制台展示文字、错误踪迹的具体布局、底层文件系统错误文字和典的调试显示顺序不作为跨版本机器协议。
